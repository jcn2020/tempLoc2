
pipeline {
    agent any
    triggers {
      pollSCM('') // Enabling being build on Push
    } 
    stages {
        stage("Helllo") {
            steps {
                echo "Hello "
            }  // steps 
        } // state
    }  // stages 
} // pipeline 
